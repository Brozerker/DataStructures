#pragma once
#include "Node.h"

class Heap {
	TreeNode * head;
public:
	Heap() {}
	~Heap() {}

	void populateTree(int arr[]) {}
	void populate(TreeNode * current) {}
	void insertAbove(TreeNode * toInsert, TreeNode * parent) {}
};